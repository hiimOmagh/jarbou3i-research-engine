# XR-13.0.5 — Documentation Contract Token Sweep

## Decision

XR-13.0.5 fixes the remaining no-browser static failure after the XR-13.0.4 static-contract sweep.

The browser suite, hosted evidence, archive check, and stable readiness already passed. The remaining blocker was documentation-only: the XR-13 design document used equivalent wording but missed the exact lowercase static token `visible-element inventory`.

## Scope

Changed files:

- `docs/design/xr-13-user-friendly-product-experience-recomposition.md`
- `docs/design/xr-13.0.5-documentation-contract-token-sweep.md`

## Fix

The XR-13 design document now contains an explicit static contract marker sweep with the exact required audit vocabulary:

- visible-element inventory
- repetition audit
- user-facing questions
- NOISE
- SYSTEM
- PRIMARY
- pre-import
- post-import
- Prompt Builder
- Expert Cockpit
- Editorial Export
- What should the user do next?
- runtime IDs
- export contracts
- XR-8 evidence matrix
- RC-1 remains blocked

## Non-goals

- No runtime logic change.
- No CSS behavior change.
- No browser selector change.
- No import/export/schema behavior change.
- No UX behavior change.

## Process note

This is the final missing documentation-token sweep for XR-13. It should be applied on top of the current uncommitted XR-13 + XR-13.0.1 + XR-13.0.2 + XR-13.0.3 + XR-13.0.4 state.
