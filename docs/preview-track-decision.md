# Preview Track Decision

The release root is the single source of truth. `preview/` and `biopreview/` are not valid release tracks and must not exist in the release root.

The expanded biopolitical systems model is implemented in the root app only.
